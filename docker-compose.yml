version: '3.8'

services:
  dashboard:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: strawmind_local
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
